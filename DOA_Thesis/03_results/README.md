# Results Archive

Copy only thesis-ready outputs here. Keep raw training directories in `myDOA/train/results/` and `myDOA/test/results/`.

## Subdirectories

| Directory | Content |
| --- | --- |
| `snr/` | RMSE/SR vs SNR CSV and figures. |
| `ablation/` | Exp1-Exp5 ablation tables and figures. |
| `snapshot/` | Snapshot robustness results. |
| `angle_separation/` | Minimum angle separation results. |

## Result Record Template

For every copied result, record:

- Source path:
- Model/checkpoint:
- Test command:
- Metrics:
- Used in thesis figure/table:

