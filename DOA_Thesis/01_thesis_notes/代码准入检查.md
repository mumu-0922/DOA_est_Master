# 代码准入检查

目的：在重新训练前，先判断当前 `myDOA` 代码是否能支撑论文实验。旧代码效果不好时，不能直接继续堆参数，必须先把数据、模型、指标和保存逻辑查清楚。

## 1. 数据生成检查

| 检查项 | 文件 | 通过标准 | 状态 |
| --- | --- | --- | --- |
| ULA 导向矢量 | `data/signal_datasets.py` | 与第 2 章阵列模型一致 | 待查 |
| SNR 定义 | `data/signal_datasets.py` | 信号功率、噪声功率定义清楚 | 待查 |
| SCM 计算 | `data/signal_datasets.py` | `R_hat = YY^H / T` 或等价形式明确 | 待查 |
| DOA 生成 | `data/signal_datasets.py` | 角度范围、最小间隔、排序规则明确 | 待查 |
| 目标谱生成 | `data/signal_datasets.py` | 高斯峰宽、归一化方式固定 | 待查 |
| 2/4 通道输入 | `data/signal_datasets.py` | 只差输入通道，不改变标签或数据分布 | 待查 |

## 2. 模型检查

| 检查项 | 文件 | 通过标准 | 状态 |
| --- | --- | --- | --- |
| Vanilla CNN | `models/vanilla_cnn.py` | 2 通道、无注意力、作为基线 | 待查 |
| 4 通道无注意力 CNN | `models/ca_doa_net.py` 或新模型 | 与 CA 模型主干可比，仅关闭注意力 | 待查 |
| CA-DOA-Net | `models/ca_doa_net.py` | 注意力插入位置明确 | 待查 |
| 模型容量 | 训练日志或脚本 | 对比模型参数量差异可解释 | 待查 |

## 3. 损失函数检查

| 实验 | 损失 | 通过标准 | 状态 |
| --- | --- | --- | --- |
| Exp1 | MSE | 只用谱损失 | 待查 |
| Exp2 | MSE | 只用谱损失 | 待查 |
| Exp3 | MSE | 只用谱损失，验证 CA | 待查 |
| Exp4 | MSE + Sparse | 验证谱稀疏约束 | 待查 |
| Exp5 | 完整损失 | 每个损失项可解释、可开关 | 待查 |

## 4. 测试指标检查

| 检查项 | 文件 | 通过标准 | 状态 |
| --- | --- | --- | --- |
| 峰值搜索 | `models/base_network.py`, `test/*.py` | 所有深度模型统一 | 待查 |
| MUSIC/ESPRIT 匹配 | `test/compare_methods.py` | 与深度模型同一匹配规则 | 待查 |
| RMSE | `utils/metrics.py`, `test/*.py` | 使用最优匹配后计算角度误差 | 待查 |
| Success Rate | `utils/metrics.py`, `test/*.py` | 容差阈值固定，例如 2 deg | 待查 |

## 5. 保存与复现检查

| 检查项 | 通过标准 | 状态 |
| --- | --- | --- |
| 保存 config | 每次训练完整保存参数 | 待查 |
| 保存 seed | 训练和测试 seed 都记录 | 待查 |
| 保存 best model | 验证集指标定义明确 | 待查 |
| 保存 CSV | 每个 SNR 的 RMSE/SR 保存 | 待查 |
| 保存图 | RMSE/SR 曲线保存 | 待查 |
| 命令记录 | 结果目录内保存训练/测试命令 | 待查 |

## 6. 下一步执行顺序

1. 先读并审计 `data/signal_datasets.py`、`models/ca_doa_net.py`、`models/vanilla_cnn.py`、`utils/loss_function.py`、`utils/metrics.py`、`test/compare_methods.py`。
2. 标出不符合论文定义的地方。
3. 先修数据与指标，再修模型。
4. 用小样本跑最小闭环。
5. 最小闭环通过后，再跑正式实验。

